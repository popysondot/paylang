services:
  - type: web
    name: paylang-api
    repo: https://github.com/yourusername/paylang
    branch: main
    rootDir: .
    buildCommand: npm install && cd server && npm install && cd .. && npm run build
    startCommand: node server/index.js
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
      - key: VITE_BACKEND_URL
        value: https://paylang-tusk.onrender.com
